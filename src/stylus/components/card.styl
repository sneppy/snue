/* 
	Card stylesheet
 */
.card
	padding: 1.5em

	box-shadow: 2px 2px 2px 2px rgba(black, 0.2)

	& > .title
		margin-top: 0.5em
		margin-bottom: 1em

.card-column
	display: flex
	flex-direction: column

	padding: 1em

	& > .card:not(:last-of-type)
		margin-bottom: 1em