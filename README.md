# snue
A simple Vue components library

## Installation

```shell
npm i --save snue
```

## Using the library

Import it in the `main.js` script and use it:

```javascript
import snue from 'snue'

Vue.use(snue)
```